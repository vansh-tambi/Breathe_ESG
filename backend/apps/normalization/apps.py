from django.apps import AppConfig


class NormalizationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.normalization'
    verbose_name = 'Data Normalization & Conversion'
